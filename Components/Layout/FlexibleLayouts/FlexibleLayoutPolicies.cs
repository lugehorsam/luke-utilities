using UnityEngine;
using System.Collections;

public enum FlexibleSpacingPolicy {
    None,
    Collider2D,
    Renderer
}

public enum FlexibleFlowPolicy {
    None = 0,
    Horizontal = 1,
    Vertical = 2
}