namespace Utilities
{
    public enum FlexibleSpacingPolicy {
        None,
        Collider2D,
        Collider,
        Renderer
    }

    public enum FlexibleFlowPolicy {
        None = 0,
        Horizontal = 1,
        Vertical = 2
    }   
}
